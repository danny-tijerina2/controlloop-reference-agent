[project]
name = "support-desk"
version = "0.1.0"
description = "Deliberately over-privileged OpenAI Agents SDK demo for the ControlLoop reference scenarios."
requires-python = ">=3.12"
dependencies = ["openai-agents>=0.2.0"]
