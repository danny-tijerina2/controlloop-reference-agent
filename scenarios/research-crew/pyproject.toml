[project]
name = "research-crew"
version = "0.1.0"
description = "Deliberately over-privileged CrewAI demo crew for the ControlLoop reference scenarios."
requires-python = ">=3.12"
dependencies = ["crewai>=0.86.0"]
